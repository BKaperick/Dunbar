
" Can be set to `:verbose` to print every query to console (useful for debugging sql syntax issues "
const global_sql_mode = :empty
const pag_result_table = "PagResult"
const benchmark_timing_table = "BenchmarkTiming"

